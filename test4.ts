import * as dotenv from 'dotenv'
dotenv.config({ path: '.env.local' })

async function run() {
  const storeId = '47ced718-4b74-45d7-a95e-34da98308723';
  // Simulate the API call to localhost (assuming it's running, wait, we are not running a local server)
}
run();
