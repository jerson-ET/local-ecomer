import './calzado.css'

export const metadata = {
  title: 'SNEAKER VAULT - Streetwear & Kicks | LocalEcomer',
  description: 'Los drops más exclusivos. Nike, Jordan, Yeezy y más.',
}

export default function CalzadoStoreLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
